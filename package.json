{
  "name": "atlas-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Atlas Automation \u2014 service-based media/broadcast automation platform. Nx monorepo: shared libraries under libs/, deployables under apps/, design docs under docs/.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "libs/*",
    "apps/*",
    "spikes/*"
  ],
  "scripts": {
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint",
    "typecheck": "nx run-many -t typecheck",
    "build": "nx run-many -t build",
    "affected:test": "nx affected -t test",
    "affected:lint": "nx affected -t lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "api:types": "node scripts/generate-api-types.mjs",
    "api:check": "node scripts/generate-api-types.mjs --check",
    "graph": "nx graph",
    "lock:check": "npm ci --dry-run",
    "verify": "npm run lock:check && npm run format:check && npm run api:check && npm run lint && npm run typecheck && npm run test",
    "k8s:build": "docker build -f infra/docker/Dockerfile --build-arg SERVICE=iam -t atlas/iam:dev . && docker build -f infra/docker/Dockerfile --build-arg SERVICE=mam -t atlas/mam:dev . && docker build -f infra/docker/Dockerfile --build-arg SERVICE=api-gateway -t atlas/api-gateway:dev .",
    "k8s:load": "kind load docker-image atlas/iam:dev atlas/mam:dev atlas/api-gateway:dev --name atlas-dev",
    "k8s:deploy": "kubectl apply -k infra/k8s/overlays/dev",
    "k8s:up": "npm run k8s:build && npm run k8s:load && npm run k8s:deploy",
    "k8s:observability": "kubectl apply -k infra/k8s/observability",
    "smoke": "node --test infra/smoke/smoke.test.mjs",
    "bundle": "node scripts/build-bundle.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.13.3",
    "eslint": "^10.8.1",
    "nx": "^23.1.0",
    "prettier": "^3.4.0",
    "tsx": "^4.23.12",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.67.0",
    "yaml": "^2.9.0"
  },
  "overrides": {
    "nx": {
      "brace-expansion": "^5.0.9"
    }
  }
}
