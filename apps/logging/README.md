# @atlas/logging — the audit sink

Every event on the bus, appended once, hash-chained per channel; and the per-entity change history
the diff viewer reads. EP-19.1. Design:
[logging-analytics.md](../../docs/architecture/services/logging-analytics.md).

Generated by `nx g service logging --db --broker` — the generator's first real run — and then given
its domain. What the scaffold provides (probes, metrics, correlation, the problem document, drain)
is in [`tools/generators/README.md`](../../tools/generators/README.md); this file is about the sink.

## What it does

```
broker  atlas.>  ──▶  ingest()  ──▶  audit_events     (every envelope, append-only, hash-chained per channel)
                         └────▶  entity_history   (one row per audit.recorded revision)
                                                          ▲
GET /api/v1/history/{entityType}/{id}  ◀──────────────────┘   logs:read AND <entityType>:read
```

**One message is one transaction.** Claim the message id (`PgSeenStore.mark`, in the transaction —
EP-03.3), read the channel's chain head, append the record with `hash = sha256(prevHash + canonical
record)`, and for an `audit.recorded` event project the history row. All of it commits or none of
it does, which is what turns at-least-once delivery into exactly-once append: a crash between the
claim and the append rolls both back and JetStream redelivers. A redelivery of something already
appended is a `duplicate` and does nothing.

**Malformed input is thrown, not skipped.** A message that is not an envelope, or an `audit.recorded`
payload that fails its schema, will never become valid. Throwing lets the broker retry and then
dead-letter it — where `scripts/dlq.mjs` shows it to a person. Skipping would ack a message the audit
log then silently lacks, which for a sink is the one outcome worse than a poison message.

## The store is a port, with two adapters and one suite

`AuditStore` — `sqliteAuditStore` for tests and dev, `pgAuditStore` for production — held to
`auditStoreConformance`, which runs against **both** in CI (Postgres from the workflow's service).
The suite is driven through `ingest`, because the properties that matter are properties of the
unit of work:

| Property                             | How the suite proves it                                                                            |
| ------------------------------------ | -------------------------------------------------------------------------------------------------- |
| Appended once                        | the same message twice: `appended`, then `duplicate`; one row                                      |
| Chain gapless per channel, verifies  | five appends, `seq` 1..5, `verifyChain` ok; another channel starts at 1                            |
| Tamper-evident                       | alter one record in memory → `verifyChain` fails **and names the seq**                             |
| **Append-only, in the database**     | the harness issues a raw `UPDATE` around the port → the trigger refuses                            |
| History projected, in revision order | two revisions in, two out, channel-scoped, deep-linked by `messageId`                              |
| Atomic                               | a payload failing its schema rolls back the record **and the claim** — the redelivery is processed |

> ⚠️ **The design's hot store is OpenSearch** (EP-07.4, open). This is the MAM precedent instead: a
> Postgres adapter behind the port, so the port and the suite are the contract and OpenSearch is an
> adapter when it arrives. Two consumers appending to one channel at once collide on
> `UNIQUE (channel_id, seq)`; the loser rolls back, its claim with it, and is redelivered to append at
> the next seq. Optimistic, and correct — and with one replica in dev, never exercised.

## The read surface

`GET /api/v1/history/{entityType}/{id}` — the revision timeline, oldest first, each entry with the
field-level `delta`, the actor, the correlation id and the `messageId` of the envelope it came from.

Two permissions, **both** enforced with `canEnforce`: `logs:read` in the caller's channel, and
`<entityType>:read`. A history is the entity's past states; whoever may not read the asset may not
read what it used to say, and whoever may read assets is not thereby an auditor. The history of an
unknown entity is an **empty** timeline, not a 404 — a 404 would confirm to anyone who may read the
channel that no such entity ever existed there.

Routed through the gateway (`/api/v1/history`, `/api/v1/logs` — in `apps/api-gateway/src/main.ts`,
the production table; `routing.ts`'s `defaultRoutes` is a test fixture); the smoke suite gates on it being
reachable and then asserts the whole spine: **a write through the gateway appears in its own audit
history, with its delta, on a live cluster.**

## The log browse (EP-19.3)

`GET /api/v1/logs` (filter in the query string) and `POST /api/v1/logs/query` (the same filter as
JSON, which is where a list of types fits) — one engine behind both. One page of the caller's
channel's log, **newest first**, keyset on `seq`: pass `nextCursor` back as `before`.

**Permission-filtered, all retained** (FR-LOG-2). `logs:read` is the door; each entry is then
shown only if the caller holds the permission _it_ requires — `<domain>:read` for an event under
`atlas.<ch>.<domain>.…`, the **entity's** read permission for an `audit.recorded` delta. This is the
websocket service's eligibility rule, on purpose: what you may receive live and what you may read
back are the same question, and answering it two ways would let one drift wider. The websocket
service's `mayReceive` and this service's `visible` are the two places; `requiredPermission` is
tested against both conventions.

Filtering happens **after** the read, so a page can come back thin — even empty — with a cursor
still present. That is normal, not the end of the log. The cursor advances per row _considered_,
not per row returned: advancing only on visible rows would re-scan filtered ones forever, and
advancing past what was read would skip rows the loop never reached (MAM's listing has the same
rule and the same reason). A test walks three pages through a log where the caller may see one
row in five.

Filters: `type` (comma-separated) / `types`, `correlationId` — the **request view**, everything one
request caused, which the smoke suite proves on a live cluster — `actorId`, `from`/`to`, `before`,
`limit` (max 200). A malformed filter is a 422 problem document.

## Not fanned out

`audit.recorded` is **not** delivered to WebSocket subscribers: the websocket service maps
`atlas.<ch>.audit.*` to `audit:read`, which no role grants, so eligibility fails closed. Deliberate —
the delta carries whole field values, including field groups a reader may not hold. This service is
the consumer.

## Not yet

- **19.4** retention and cold-storage tiering.
- `user.>` (private per-user streams) is not sunk by default. Adding it is one pattern; deciding it
  is a privacy question.
- A `verifyChain` endpoint for compliance to call. The function exists; the route is a decision.

## Run

```sh
npx nx test @atlas/logging                                  # sqlite, in memory
ATLAS_PG_URL=postgres://atlas:atlas@localhost:55432/atlas npx nx test @atlas/logging   # + Postgres
npm run k8s:up && npm run smoke                             # the spine, on kind
kubectl -n atlas logs deployment/logging -f
```

| Variable              | Default                            |                                                                           |
| --------------------- | ---------------------------------- | ------------------------------------------------------------------------- |
| `ATLAS_PG_URL`        | `postgres://…@postgres:5432/atlas` | waited for at startup, within a 120 s budget                              |
| `ATLAS_NATS_URL`      | `nats://nats:4222`                 | retried, never a readiness check — JetStream retains what it has not seen |
| `ATLAS_IAM_ORIGIN`    | `http://iam:3000`                  | critical readiness dependency; the policy client                          |
| `ATLAS_POLICY_TTL_MS` | `30000`                            | the revocation window for cached policies                                 |

Metrics: `atlas_audit_events_appended_total`, `_duplicate_total`, `_refused_total` — by service
only, never by subject (a subject carries the channel id).
