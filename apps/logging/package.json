{
  "name": "@atlas/logging",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The audit sink: every event on the bus, appended and hash-chained; the per-entity change history the diff viewer reads.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --no-warnings --import tsx --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test"
  },
  "dependencies": {
    "@atlas/contracts": "*",
    "@atlas/data": "*",
    "@atlas/data-opensearch": "*",
    "@atlas/data-pg": "*",
    "@atlas/messaging": "*",
    "@atlas/messaging-nats": "*",
    "@atlas/policy": "*",
    "@atlas/service-kit": "*",
    "fastify": "^5.12.4"
  },
  "devDependencies": {
    "tsx": "^4.23.13"
  }
}
