node_modules/
dist/
.nx/
coverage/
package-lock.json

# The reference implementation is frozen and already validated (84 tests).
# It is formatted when each package is lifted into libs/ (EP-02.1, EP-03.1, ...).
reference/

# Docs are hand-formatted markdown with deliberate table alignment and
# line breaks; prettier would reflow them and destroy the diffs.
docs/

# Golden fixtures must stay byte-exact.
**/*.golden.*

# Local agent/editor state — not ours to format.
.claude/

# The Helm chart is GENERATED from infra/k8s/base (ADR-0006) and is YAML with Go template
# expressions in it — prettier cannot parse `{{ ... }}` as a value, and reformatting a generated
# file would only make `npm run k8s:check` fail. Change the manifests, not the chart.
infra/helm/
