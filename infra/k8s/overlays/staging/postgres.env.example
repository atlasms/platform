# Copy to postgres.env (git-ignored) and set a real password before `kubectl apply -k`.
# The base names the Secret `postgres-credentials` and reads `username` and `password` from it.
username=atlas
password=change-me-before-apply
