# AGENTS.md — how to continue this project

**Read this file first. It is the entry point.** It exists so you can pick up work without
reading the whole repository. Everything below is current as of the last commit on `main`.

---

## 1. What this is

**Atlas Automation** — a service-based media/broadcast automation platform (TV, radio, news, live
events): ingest → transcode → catalogue → review/approve → schedule → send-to-air, plus newsroom,
workflow, feeds and AI enrichment.

This repo (`atlasms/platform`) is **one Nx monorepo** holding the design docs _and_ the code.

- `docs/` — the design. Extensive and authoritative. **It is the source of truth, not the code.**
- `libs/` — shared TypeScript libraries (`@atlas/*`).
- `apps/` — one deployable per service, plus `studio` (the Angular SPA) and `walking-skeleton`.
- `reference/` — a **frozen, validated prototype** (84 passing tests) that is being lifted into
  `libs/` package by package. Do not develop here; lift from it.
- `scripts/` — GitHub project/backlog automation, the CI checks (`api:check`, `graph:check`, `tier0:check`, `schema:check`, and `check-changelog` on pull requests), and **`setup.sh` / `setup.ps1`** — a new machine from nothing to smoke-green, requirements check first ([`SETUP.md`](SETUP.md)).
- `tools/` — local Nx generators. **`npx nx g service <name>` is how a service starts** ([`tools/generators/README.md`](tools/generators/README.md)).

## 2. Orient yourself in ~10 minutes

(A new machine? [`SETUP.md`](SETUP.md) — requirements, every manual step, and the script that does them.)

Read these four, in order. Do **not** read the whole `docs/` tree.

| #   | File                                                                                             | Why                                                                        |
| --- | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------- |
| 1   | [`docs/roadmap/20-delivery-process.md`](docs/roadmap/20-delivery-process.md)                     | How we work: cadence, board, **Definition of Done**, commit/PR rules.      |
| 2   | [`docs/roadmap/21-epic-breakdown.md`](docs/roadmap/21-epic-breakdown.md)                         | **The backlog.** 46 epics; Phases 0–1 fully storied. Your task is in here. |
| 3   | [`docs/roadmap/16-system-implementation-plan.md`](docs/roadmap/16-system-implementation-plan.md) | Build order, foundations, cross-cutting engineering standards.             |
| 4   | [`docs/README.md`](docs/README.md)                                                               | Index of every other doc + the **Assumptions Register** (A1–A12b).         |

Then read **only** the specific docs your task touches — e.g.
[`docs/architecture/services/<service>.md`](docs/architecture/services/) plus its
[build plan](docs/roadmap/services/) and [OpenAPI stub](docs/architecture/openapi/).

## 3. Where the project currently stands

**Phase 0 (Foundations), iteration S01.** Planning is complete; the build has just started.

| Item                             | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| -------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Design docs                      | ✅ Complete                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| GitHub backlog                   | ✅ 175 issues on org project **#2**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **EP-01** foundations            | ✅ **Complete** — monorepo, CI, CODEOWNERS, containers, K8s ([ADR-0002](docs/adr/0002-deployment-target.md)), [offline bundle](docs/operations/offline-bundle.md), and the **Helm chart for customer installs** ([ADR-0006](docs/adr/0006-helm-chart.md)) — GENERATED from `infra/k8s/base` by `scripts/build-helm-chart.mjs`, so the two deployment paths cannot drift: `k8s:check` re-runs the generator, diffs it against what is committed, `helm lint`s, renders the chart twice (defaults and production-shaped) and holds both to the manifests' conventions plus the chart's own (registry images, no seed, no literal credential, a PDB for every multi-replica deployment and none for a single-replica one, TLS `/ws` + `/`), then asserts every workload in the manifests is in the chart. Values cover images/registry/pull secrets (data plane included), scalable replicas, resources, storage size and class, each data-plane component's enabled/host, the credentials Secret and its keys, and the Ingress; IAM/RIM/data-plane replicas are deliberately NOT values. **01.3 consumer fanout is guarded**, not just true: `graph:check` fails CI if any package's imports are not edges in the graph `nx affected` walks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |     |
| **EP-02** `@atlas/contracts`     | ✅ 8 tests — loads all 53 schemas from `docs/`. **02.3 generated payload types** — `EventPayloads['asset.created']` etc. and the shared `$defs`, projected from the same schema files the validators load (`npm run api:types`; `api:check` fails CI on drift). Generating them surfaced a field spelt two ways across two IAM events and the websocket bridge reading `userId` one envelope level too high. **02.5 Tier-0 enums** — every schema `enum` is inventoried with its reason in `schemas/tier0-enums.json`, `tier0:check` fails CI on a new, stale or twice-defined one, and the shared ones are exported as values (`TierValues`) with the type derived. Consolidating found "task kind" defined three ways with two value sets. **02.6 semver + changelog** — `libs/contracts/CHANGELOG.md` (Keep a Changelog; the surface is the exports AND the schemas; while `0.x` a minor is the breaking bump), version `0.2.0` covers everything since the lift, and CI refuses a PR that touches `src/` or a `*.schema.json` without touching the changelog (`check-changelog.mjs`, PR-only). **02.4 operations table** — `generated/*.operations.ts` in Studio: verb, path as served, path params per `operationId`; `ApiClient.call(op, { params }).as<T>()` builds every request from it, the type requires exactly the path's params, `api:check` holds it. No Studio service spells a URL. Generating it found `iam.yaml` putting `/auth/*` and the JWKS under `/api/v1` (path-level `servers` now say root, as the gateway serves them), a login body field spelt `login`, and MAM serving `PUT /field-schemas` and `/assets/{id}/extended` that the contract did not have — all corrected. ⬜ 02.2                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **EP-03** `@atlas/messaging`     | ✅ 38 tests + `@atlas/messaging-nats` on **real JetStream** (15), on the `@nats-io/*` client (#207). [ADR-0001](docs/adr/0001-message-broker.md). **03.4 DLQ inspection + replay** (`scripts/dlq.mjs`). **03.7 relay pipelining** (6.9x measured). **03.3 consumer idempotency** — the race was in the INTERFACE (`seen` then `remember` cannot be made atomic by any store), so the port is now one `markSeen`, with a conformance suite the in-memory, sqlite and Postgres stores all run. **`broadcast: true`** subscriptions (EP-17.7) — every instance gets every message, from now on, once: an ephemeral JetStream consumer, not durable/retried/dead-lettered; for cache invalidation and fan-out, never for work. ⬜ 03.5 causation propagation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **EP-04** `@atlas/service-kit`   | ✅ 74 tests — errors, config, auth, health, logging, metrics, alerts, **04.7 tracing** ([ADR-0004](docs/adr/0004-tracing-implementation.md): W3C Trace Context + OTLP by hand). 04.2–04.5 closed with evidence. **04.6 RFC 9457** — one problem document, `application/problem+json`, `type`/`title`/`status`/`detail`/`instance` derived from the platform's `code`/`message`/`correlationId`, which stay; every responder, every stub, the generator's template, Studio's generated `Error` (now a closed `code` union), and a smoke assertion on the deployed gateway. **04.9 `nx g service <name>`** — scaffolds a service that passes this repo's bar on its first run (six tests come with it) AND wires it into `k8s:build`/`k8s:load`, the base kustomization and the dev overlay — the list #298 fell out of. Derived from the four services, not designed; deriving it found the websocket drift (#313). CI runs it for real on every PR and holds the output to lint/typecheck/test                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **EP-05** `@atlas/policy`        | ✅ 28 tests, driving Studio rendering, and **05.5/05.6**: the evaluator is bundled for the browser and run in a `vm` realm with NO Node globals, against the shared decision table, in both lenient and strict mode — plus a 2560 B gzipped size budget (~1.1 kB today)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **EP-06** `@atlas/reference`     | ✅ 33 tests incl. **06.6 seed-as-code** — `@atlas/reference/seed`, a Node-only SUBPATH so the main entry stays browser-safe. Additive and idempotent: an operator's edit is `preserved`, never overwritten. No caller yet (nothing stores settings rows), so it is built and unwired                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **EP-07** `@atlas/data`          | ✅ 23 tests + `@atlas/data-pg` on **real Postgres** (21) — outbox persists message **headers**, and a `seen` table now carries the outbox's other half: a consumer's dedup mark commits in the SAME transaction as its writes, so a crash mid-handler cannot suppress the redelivery. **07.4 OpenSearch** — `@atlas/data-opensearch` (client, readiness at yellow, idempotent `ensureIndex`) and the audit sink's hot index behind it: Postgres stays the record (trigger, chain, transactional seen-mark), a projector copies rows in chain order keyed by `messageId` and resumes from the index's own `max(seq)` per channel — no checkpoint table, delete-and-rebuild is an operation. `GET /logs` reads the index when configured; one `browseConformance` runs on both readers; `503 UNAVAILABLE` is a new taxonomy member for a dependency that is away. ADR-0005. Redis/Mongo wait for a consumer. OpenSearch is in CI (service), kind and Smoke. **07.6 schema ownership** — every service opens its pool on its OWN Postgres schema (`openPool({ schema })`, `ATLAS_PG_SCHEMA`, created by `migrate()` under the lock). Found on the way: all four services were in `public` of the one database, so `outbox` was ONE table drained by four relays with no claim locking — MAM's rows published by IAM, twice, out of subject order, hidden by consumer dedup. **07.5 the data plane as code** — `infra/k8s/overlays/staging`, the production shape: registry images by release tag (CD pins the digest), two gateway/MAM replicas behind PDBs, a sized data plane, credentials from a Secret generated from a file git never sees, ONE TLS Ingress (`/ws` → websocket, `/` → gateway), no seed. `npm run k8s:check` (`scripts/check-k8s.mjs`, in `verify` and CI) renders every overlay and asserts the conventions — probes, requests, a read-only root, and staging's shape — so a manifest regression fails in CI, not on a cluster. No Terraform: ADR-0002 keeps the toolchain at `kubectl`; the cluster, storage classes, DNS and the certificate are the environment's. `schema:check` (`tables.json`) holds every `CREATE TABLE`: one owner, both adapters, `channel_id NOT NULL` or a written reason, owner-prefixed migration ids. ⬜ 07.5                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **EP-08** `api-gateway`          | ✅ 47 tests, routes MAM, proxies bodies byte-transparently, **08.3 rate limiting + size caps** (per replica — see README). **08.5 aggregated `GET /reference`**. **08.6 access log** — one structured line per request to the log pipeline, and it IS the `gateway.access.logged` contract: a test validates the real record against the schema (it did not validate before — #245 added `route` and nothing compared them). The inbound correlation id is adopted only when it is a well-formed ULID                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **EP-09** `websocket`            | ✅ 37 tests — registry/eligibility/bridge, and since **EP-13.2 a real `/ws` server**: token verified BEFORE the upgrade (a bad one is a 401 with a body, not a silently closed socket), policy resolved fail-closed, heartbeat, `/ws/stats`. **Deployed** — k8s manifest, dev NodePort 30081, connection caps (node-wide + per user, since the gateway no longer bounds this path), and a smoke test that drives the whole path. It is **not** behind the gateway: `fetch` cannot upgrade a protocol, so `/ws` is an ingress path rule (one origin to the browser) rather than a gateway route — which also keeps long-lived sockets out of a stateless proxy tier. **09.4 reconnect + polling fallback** — no replay window (Redis deferred, ADR-0005), so the client does what §6.2 prescribes when a gap exceeds the window: on every reconnect it re-subscribes then emits `resync$('reconnected')`, and while down it emits `poll` every 30 s (only with a subscriber, only when visible); every live panel/editor refetches what it shows, editors never over unsaved edits. Jittered backoff (`[cap/2, cap)`), a client `ping`/`pong` heartbeat (the browser hides the server's), `reconnecting` held across attempts, `connect()` takes over a pending timer, a throwing constructor reconnects; status bar shows `● live` / `○ polling`. `resume` is still refused BY NAME rather than acknowledged and ignored.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **EP-10** `iam`                  | ✅ 77 tests, incl. `/metrics` + auth signals (#205) and failed-attempt lockout (#240). **10.4 (part 1) persistence** — `IamStore` is a port with sqlite + Postgres adapters and one conformance suite driven through the service: a username taken exactly once, the login's record + event + token in ONE transaction, a refresh token rotated exactly once whoever asks (two concurrent refreshes: the database lets one through, the other is a reuse — `revokeTokens` returns the count), lockout state persisted, the login trail append-only by trigger, the seed idempotent on restart. Groups hold roles BY ID; assignments are addressable. **10.4 (part 2) admin CRUD** — `IamAdmin` (`admin.ts`): users, groups, memberships, roles, assignments over HTTP per `iam.yaml`, every operation `canEnforce`d for `user:admin` in the ROW's channel (a platform-wide row needs an unscoped grant; a channel admin cannot write a rule that reaches beyond their channel), another channel's row is NOT FOUND. **10.6 emission** — IAM has an outbox relay now: a grant, a membership, a role edit, a disable write the rows, the `audit.recorded` delta and `permissions.changed` (permVersion bumped, one per affected user — a role edit reaches every holder, directly or through a group) in ONE transaction; memberships also emit `group.membership.changed`. Deleting a role still granted is refused, and **`GET /roles/{id}/holders`** surfaces that same question: the groups that carry it and every person it reaches, one row each carrying both paths, a direct holder's row carrying the assignment id so a revoke is one call. It needs an unscoped grant for a platform-wide role, whose holders span every channel. `IamStore.roleHolders` replaced the walk over every user in the estate (and its silent `limit: 10_000` ceiling) with the assignment index plus a GIN containment query on the group document. The logging sink reads IAM's trail under `user:admin` (`visibility.ts`). Smoke: a grant's three envelopes cross the spine into the audit log                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **EP-13** walking skeleton       | ✅ 12 tests incl. **13.3 one trace across gateway → service → broker → consumer** + **13.4 smoke suite green** (24/24, run against kind **and now in CI** — see below). **13.2 COMPLETE and OBSERVED** — the last hop, outbox → broker → bridge → socket, is asserted by a smoke test that holds a real socket open while a write goes through the gateway, and it passes against a live cluster.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **EP-11** Studio shell (Angular) | ✅ 11.1 skeleton, **11.2 real sign-in against IAM** (full-screen, a top-level route; the workbench is the guarded parent of every panel, so nothing renders without a session and a deep link comes back through `returnUrl`), 11.3 the workbench, **11.4 WebSocket client** (queued subscriptions, backoff reconnect; **the `/ws` endpoint it talks to now exists** — EP-13.2 — but is not yet routed through the gateway), **11.6 i18n/RTL** (en/ar + design tokens; the token set is now COMPLETE and the RTL mirroring reaches the side-bar divider and the panel names), 11.7 `can()` rendering — 112 tests. **11.5 generated API TYPES** — every contract Studio has a client for is in `SPECS`, RIM included (`npm run api:check`). The clients themselves (`AssetsService` etc.) are hand-written on those types, with hand-typed paths nothing compares to the stubs' `paths` — that half is EP-02.4 (#57), open for a design call.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **EP-20** Studio panels          | ✅ **20.1 Media panel** — recent/search/tag filters against real MAM. **20.2 asset editor** — real Basic-info GET/PATCH, per-field-group rendering, dirty state, Files rendition readiness; **per-file rows** from MAM's FileRef mirror (EP-17.8) — kind, tier, status, size, checksum, path; read on entering the tab and again on a live event. **20.3 Ingest panel** COMPLETE — the queue (a keyset page, newest first), the quarantine review, and the **uploader** against RIM's chunked upload: the server sizes the parts and the file is sliced exactly so, sent one part at a time as octet-stream with in-part progress; a part that fails on the way is retried three times, one the server refuses is not; a failed transfer keeps its `uploadId` and Retry sends only what `GET /uploads/{id}` says is missing; a swept upload starts over; cancel aborts in flight and abandons server-side; completion polls `GET /ingest/{id}` to the verdict, which the row then shows. **20.8 transfer tray** — in the workbench frame (an upload outlives the panel that started it), bottom corner, minimizable, grouped percentage over the active transfers, per-row cancel/retry/dismiss and the job's reason. Every request of every path is asserted by method and URL against a fake gateway (`upload.service.spec.ts`). **20.7 user management (basic)** — the Admin panel's first view, Users, revealed by `user:admin`: keyset list, create, and a user as an editor tab — display name (the one dirty field), disable/enable/unlock with the consequence on the button, a new password, and the direct grants (a role, or an inline rule scoped to the user's channel with a browser-minted ULID) — each write its own request against IAM's admin API, since each is its own audited mutation there; disabling yourself is refused before IAM refuses it. **Groups and Roles views** — same shape (list, create, editor tab): a group's profile, members (IAM's member calls, offered from users not yet in it) and grants (roles + inline rules PATCHed as the whole set); a role's profile and rules; a platform-wide role is shown read-only; deleting a held role is IAM's 409, shown as such. The role tab also shows and edits **who holds it** (`GET /roles/{id}/holders`): groups and people, each person once with the groups they come through, granted and revoked from one select — a group is a PATCH of its roles, a user a grant, and only a direct grant is revocable there. Holders are re-read after each write rather than reconstructed in the browser. Field schemas/theme are the panel's later views. **20.4 Search panel**, **20.6 Dashboard** (state counts from MAM's `GET /assets/counts` aggregate; a reader whose grant is narrowed is refused it and falls back to the capped page walk, labelled approximate), **20.9 live updates** wired client-side (panels subscribe via the queued WS client; the EP-13.2 server exists, and end-to-end now waits only on routing `/ws` through the gateway). Browse tree blocked on #260 (categories are a field, not an entity). **20.5 schedule editor v0** — the Schedule panel (pick a day, open or create its program table) and the reel tab: add/move/resize/remove, one `PUT …/items` save. The EDITOR owns reel correctness (`editors/reel.model.ts`, pure, 18 cases): reflow off fixed anchors, overlaps refuse the save, gaps flagged; a live item's sub-reel travels with it. `scheduling.yaml` joined the generated types + operations (the generator learned `allOf`). Wall-clock in the schedule's zone via `Intl`. Sub-schedule editing is v1. |
| **EP-17** `mam` (Phase 1)        | ✅ 230 base tests (+39 Postgres) — **every mutation emits `audit.recorded` with a before/after delta in the same transaction (EP-19.2)**, — asset core, lifecycle, metadata gate, outbox events, Postgres, deployed, **17.2 extensible metadata**, **17.3 free-form tags**, **17.4 search**, field-group scoping (#225), **state-counts aggregate** (unscoped readers only — it cannot apply the per-asset check). **17.8 the FileRef mirror** — MAM's FIRST broker consumer: `transcode.completed` and `file.placed` become `FileRef` rows (one per asset/kind/variant; HSM stays the record), each message one unit of work — `AssetTx.markSeen` (the SeenStore, in use in MAM at last), the rows, the `hasRenditions` bump, an `audit.recorded` per row — so a redelivery is a duplicate and a crash a retry; a message it cannot apply (unknown asset, wrong type, off-schema) is thrown to the broker, never skipped. `GET /assets/{id}/files` behind `asset:read`/`files`; Studio's asset editor renders the rows. The producers (HSM, MTS) do not exist, so smoke cannot drive it; the consumer tests do. **17.7 read cache** — in-process LRU+TTL (`cache.ts`, an async `AssetCache` port so Redis/Valkey can stand in) over the record, the raw extended document and the tags — not the files, which have no cross-replica signal; a miss is never cached. Invalidation: evict after every commit here; every replica evicts on `atlas.*.audit.recorded` for MAM's asset entities in **broadcast** mode; the TTL (10 s) bounds what a broker outage delays. **Mutations never read from it** — a stale base is a lost update with a valid version. `Cache-Control: no-cache` reads through (declared in mam.yaml, forwarded by the gateway); Studio sends it on every event/resync/Retry reload. `atlas_mam_cache_reads_total{family,outcome}`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **EP-19** `logging`              | ✅ **19.1 the audit sink** — `apps/logging`, the generator's first real run: every event on `atlas.>` appended once (the claim, the chain head, the append and the history projection in ONE transaction — the durable `SeenStore` from EP-03.3, in use for the first time), hash-chained per channel, **append-only enforced by a database trigger** the conformance suite tries to violate on both sqlite and Postgres. **19.2** `audit.recorded` with the field-level delta, emitted by MAM in the write transaction. `GET /api/v1/history/{entityType}/{id}` behind `logs:read` AND `<entityType>:read`, routed through the gateway, and the smoke suite asserts the whole spine: a write through the gateway appears in its own audit history with its delta, on a live cluster. **Store is Postgres behind a port** (the MAM precedent) — the design says OpenSearch, which is EP-07.4. **19.3 the log browse** — `GET /logs` / `POST /logs/query`, newest first, keyset on `seq`, permission-filtered per entry with the websocket service's rule (`<domain>:read`; an `audit.recorded` delta takes the entity's read); the cursor advances per row considered, so a thin page is normal. **19.4 retention** — a channel's policy (`hotDays`, `coldDays`, `legalHold`; `GET`/`PUT /retention-policies`, `compliance:admin`, one per channel) governs a tick that trims the OpenSearch hot index to the window and never the channel's head (the projector's checkpoint), paused by legal hold; the browse is TIERED — the index for the hot window, Postgres for anything older, strictly below the last hot row — so a record gets slower with age, never gone. Every record stays in Postgres; `coldDays` is recorded, not acted on: archive/purge wait for object storage (EP-14) and a compliance decision. The policy write is the keeper's own mutation, audited into the log itself in the transaction of the change (`appendEnvelope`). Smoke sets, reads and reads back the audit of it. ⬜ the cold half of 19.4 (archive export, guarded purge)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **EP-18** `scheduling`           | ✅ **v0 (18.1–18.4)** — `apps/scheduling`, generated then given the reel model from data-model.md §3: a schedule per channel per broadcast day, items as indexed COLUMNS with materialized `start`/`end` (§3.6), fixed anchors, one-level live sub-schedules. **The write path is THIN** (§3.4): overlaps and gaps are the editor's and v1's `/validate`, never refused on save — a test saves an overlap. The aggregate's own invariants are refused at the wire and in the service. Every write emits `schedule.updated` + `audit.recorded` in one transaction. Contract rewritten in `scheduling.yaml` (the stub's `ScheduleItem` was a placeholder that did not match §3). Store is a port with sqlite/Postgres adapters and one suite, which caught the double diverging from production (INSERT OR REPLACE cascades). Smoke saves a reel through the gateway and finds it in the audit history. ⬜ v1 = EP-31: validation, approved-only guard, MCRList serializer, send-to-air, copy-a-range                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **EP-15** `rim`                  | ✅ **15.1 chunked upload** — `apps/rim`, generated then given the upload from rim.md §6.1 and FR-ING-2. **The server sizes the parts** (`partSizeBytes`/`partCount` in the `POST /uploads` answer; 8 MiB): every part but the last is exactly that long and a part of any other length is a 422 _before_ it is stored, so the assembled file is `sizeBytes` long by construction. Resumable — `GET /uploads/{id}` says which parts are held, a part sent again wins, completion with a hole is a 409 naming `details.missing`. Completion assembles in part order, hashing the bytes it writes (the job's `checksum` is of those, not of a claim), and commits the job, `ingest.detected` and the `audit.recorded` delta in ONE transaction; a second completion returns the same job. A part is two writes that cannot share a transaction — file first, row second, so a crash between them leaves a stray file a resume overwrites rather than a row promising bytes that are not there. Staging is a PVC of RIM's own (`rim-staging`), not the asset store — HSM places the received file (EP-14; RIM never writes storage). The gateway carries a per-prefix body cap for `/api/v1/uploads` (`RouteTarget.bodyLimit`, a route per capped prefix in front of the catch-all) instead of raising the 1 MiB JSON cap everywhere. Store is a port with sqlite/Postgres adapters and one suite driven through the service with a real staging directory. Smoke pushes an 8 MiB part through the gateway, out of order, resumed, and finds the job's checksum in the audit history. **15.3 acceptance rules** — a channel's rule SETS (scope: every job, a source kind, a source; rules `container`/`minSizeBytes`/`maxSizeBytes`/`aspectRatio`, each `reject` or `quarantine`), a pure engine where the worst failure decides and an UNKNOWN (aspect ratio before the probe) quarantines rather than passes. Validation follows completion as its own transaction, guarded by the state it read (`putJob(job, ifState)`) so two validators write once — the conformance suite races them — and a sweep tick recovers a job left `detected` by a crash. Rejected bytes are discarded, the row stays; `ingest.rejected` for a hold and a refusal alike. **15.6 the queue + review** — `/ingest/queue` a keyset page by state, `GET /ingest/{id}` to poll, accept/reject on a `quarantined` job only (409 otherwise), every transition an audit revision. Studio's Ingest panel is switched ON against it. Smoke writes a rule, holds an upload with it, lists it, releases it, and reads the revisions back. **15.4 the probe** — `validating` persisted: the job is taken there (guarded by `detected`), ffprobe reads the file as a child process outside any transaction, the verdict commits guarded by `validating`; `TechnicalMetadata` on the job, the wire and the audit delta. What the tool says about the BYTES is a verdict (refused → quarantined with its words, before any rule); what goes wrong with the TOOL is not (the job stays `validating`, `/readyz` shows `ffprobe`, the tick retries). `Probe` is a port: `fakeProbe()` decides by file name for the suites; `probe.test.ts` runs the real binary over a clip ffmpeg generates — skipped without the tools, FAILED in CI without them (the workflow installs ffmpeg). ffprobe is in RIM's image only: `APK_PACKAGES=ffmpeg` on its `k8s:build` line, taken by the shared Dockerfile. Smoke builds a WAV in JS and reads its metadata back through the cluster; random bytes are held. ⬜ 15.2 watchers/recorders, 15.5 `ingest.accepted` (needs MAM's asset, EP-14)         |
| **EP-12** observability          | ✅ 12.4 alerts + golden signals everywhere; **12.1 logs + 12.2 metrics** — Prometheus/Loki/Alloy/Grafana in [`infra/k8s/observability`](infra/k8s/observability/), optional per [ADR-0003](docs/adr/0003-observability-stack.md). **12.3 tracing** (Alloy OTLP → Tempo, logs↔traces linked in Grafana).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

**686 tests across 14 test projects, all green** (counted from `nx run-many -t test`, not carried
forward), merged to `main`. A further **75 need real infrastructure** — 39 in `mam`, 21 in
`data-pg`, 15 in `messaging-nats` — and **CI now runs those too**, against a Postgres service and a
JetStream container the workflow provides.

> Both figures above were re-measured, not carried forward, and both had drifted — the previous
> counts said 666 and 56. They are a per-project sum from one `nx run-many -t test`, run twice: once
> bare for the base number, once with `ATLAS_PG_URL` and `ATLAS_NATS_URL` set, with the difference
> attributed per project. If you change the test count, re-run it the same way rather than adding
> your delta to the number here — that is how it drifted. They still skip on a laptop without Docker, but a
> missing `ATLAS_PG_URL` / `ATLAS_NATS_URL` **in CI is a hard failure**, so the infrastructure cannot
> be removed without the build saying so. Plus **24 smoke tests** against a deployed cluster, and **CI now runs them** (`.github/workflows/smoke.yml`):
> it installs a checksum-pinned kind, builds the four images with the repo's own `k8s:build`, deploys the
> dev overlay and runs the suite. On every push to `main`, and on a PR that touches `infra/`, the npm
> scripts or a service `main.ts` — the set a deployment regression actually lands in. That workflow
> exists because #298 was exactly such a regression: `k8s:up` deployed an image it had never built,
> every unit test was green, and a human found it.

> **Start here to understand how it fits together:**
> [`apps/walking-skeleton`](apps/walking-skeleton/) wires the whole spine in one process and proves
> it end to end — gateway auth → service authorization → atomic outbox → relay → broker →
> permission-checked fan-out, with one correlation id threaded through every hop. Reading that test
> file is the fastest way to see how the pieces compose.

**CI runs on every PR** (`nx affected`) and on `main` (everything), and it now **blocks merge**.
The repository is public, which is what unblocked rulesets, and
[`main` is enforced](.github/rulesets/README.md): PR required, `lint · typecheck · test` green,
no force-push, no deletion, **no bypass for anyone including admins**. A direct push to `main` is
refused server-side.

Run `npx nx run-many -t lint typecheck test` before opening a PR anyway — or enable the local hook
(`git config core.hooksPath .githooks`), which fails in ~1 minute instead of after a push, a PR and
a CI run.

⚠️ **Public repo.** Everything you commit is world-readable, including history. Secret scanning and
push protection are on, but they only catch _provider_ patterns — they will not save you from
committing a customer name, an internal hostname or a real credential in a novel format.

**The dev cluster now runs the real spine:** Postgres + NATS + OpenSearch + IAM + MAM + websocket + logging + scheduling + RIM + gateway,
with `npm run smoke` asserting the whole path from login to an atomic write, its relayed event, and
that write's own audit history read back through the gateway.
Studio signs in against it for real — `npm run k8s:up`, then `npm start -w @atlas/studio`, which
proxies `/auth` and `/api` to the gateway.

**Suggested next tasks:** `EP-14` HSM (accepted files wait in staging for it, `ingest.accepted` (15.5) needs the asset it lets MAM mint, and the audit log's cold tier needs the storage; **§5.7: two reviewers + pairing, not AI-fast-tracked**) · finish `EP-20.2` per-file rows after
EP-17.8. Two decisions are waiting on a human before code, both recorded on their issues: RFC 9457 for errors (#72) and how API clients get generated (#57). Every other open Phase-0/1 story is blocked on a service that does not exist yet — and when one is started, it starts with `npx nx g service <name>`.

> **Adapters are separate packages, held to shared conformance suites.** `@atlas/messaging` and
> `@atlas/data` keep zero (or near-zero) runtime dependencies and define the rules;
> `@atlas/messaging-nats` and `@atlas/data-pg` implement them against real servers. The suites
> (`@atlas/messaging/conformance`, `@atlas/data/conformance`, `@atlas/mam/store-conformance`) run in
> CI against **both** — the in-memory / sqlite doubles _and_ real Postgres and JetStream, which the
> workflow provides. **Add a behaviour to the suite, not to one implementation.**
>
> A service's persistence follows the same shape: `MamService` talks to an async `AssetStore` port
> with a sqlite and a Postgres adapter. **A domain service must be async end to end** — a sync
> driver can satisfy an async contract, never the reverse, and production is Postgres.

> **Real servers are available for local work:** `docker compose -f infra/docker-compose.dev.yml up -d`
> gives Postgres, NATS and RabbitMQ on non-default ports ([infra/README.md](infra/README.md)).
> **CI runs Postgres and JetStream too**, on the default ports, via the workflow — so an adapter
> change is verified against the real server on every PR, not only when whoever wrote it happened
> to have Docker running. Locally those suites still _skip_ without `ATLAS_PG_URL` /
> `ATLAS_NATS_URL`; in CI a missing one is a hard failure, because a silent skip there is
> indistinguishable from a passing suite. **Every test must still pass against `node:sqlite` and
> `InMemoryBroker`** — the doubles are the fast path, not a lesser one. RabbitMQ is not in CI: it
> exists only for the spike behind ADR-0001, which rejected it.

> **`permissions.changed` has a producer now (EP-10.6).** For a long time it had three consumers
> and none: the gateway reasoned about it, Studio's `if-can` directive re-rendered on it, and the
> WebSocket service dropped live subscriptions on it — all built, all tested, all dead in a real
> deployment. IAM emits it from its outbox since 10.6, in the same transaction as the grant, and
> the smoke suite finds it in the audit log. What is still true: MAM's and websocket's 30-second
> policy cache means a revocation lands within that TTL, not instantly — by design (FR-IAM-8).
>
> It was dead for a SECOND reason until EP-02.3: the bridge read `msg.body.userId`, and the body
> on this bus is the **envelope** — the user id is at `body.payload.userId`. Its test published a
> bare `{ userId }` so it passed, and against anything IAM will actually emit through
> `buildEnvelope` it would never have fired. Fixed, and the test now publishes a real envelope.

> **⚠️ Rule 4 says "every consumer is idempotent". There is currently ONE broker consumer.**
> It is the WebSocket bridge (`apps/websocket/src/bridge.ts`), and it does not wrap itself in
> `idempotent()` — correctly, because it fans out to the sockets this replica holds and per-replica
> dedup is the most it could ever use. The durable, transactional `SeenStore` (EP-03.3) is built,
> conformance-tested against sqlite and real Postgres, and **has no caller yet**: the consumers that
> need it — EP-15 ingest (RIM is a producer only until EP-14/15.5), EP-16 transcode — do not exist. So rule 4 is a
> requirement on the code you are about to write, not a description of what is already wired. When
> you add the first state-changing consumer, `mark(tx, id)` inside its unit of work is the whole
> point; see [`libs/messaging/README.md`](libs/messaging/README.md).

> **⚠️ If you enforce authorization, call `canEnforce`, never `can`.** Lenient `can()` treats a
> predicate it cannot check as "any", so an **incomplete context yields a WIDER grant**.
> [authorization-model.md §5.1](docs/architecture/authorization-model.md) is normative.

> **All the foundation libs are leaf nodes**; the edges live above them:
> `data → messaging`, `api-gateway → contracts, service-kit`,
> `iam → contracts, policy, service-kit`, `websocket → contracts, messaging, policy, service-kit`.

## 4. Find and claim your task

The backlog is on GitHub. `gh` must be authenticated with the **`project`** scope.

```bash
# What is ready to work on
gh issue list --repo atlasms/platform --state open --limit 20

# One story's full detail (goal, DoD, refs)
gh issue view <number> --repo atlasms/platform

# The board
gh project item-list 2 --owner atlasms
```

Issue titles are `EP-nn — Epic title` and `EP-nn.s — Story title`, matching
[doc 21](docs/roadmap/21-epic-breakdown.md) exactly. **Stories are sub-issues of their epic.**

> ⚠️ **Never guess an issue number.** Epic `EP-01` is `#1`, but story `EP-01.1` is `#47` — they are
> nowhere near each other. Always resolve the number with `gh issue list` before writing
> `Closes #n`. This has been got wrong twice.

## 5. Non-negotiable rules

These come from [the Definition of Done](docs/roadmap/20-delivery-process.md#7-definition-of-done).
Violating them silently breaks the architecture:

1. **Contracts first.** Update the [OpenAPI stub](docs/architecture/openapi/) and/or the
   [event payload schema](docs/architecture/schemas/) **before or with** the implementation. The
   schemas in `docs/` are loaded at runtime by `@atlas/contracts` — they are live code, not
   documentation.
2. **Docs ship in the same PR as the code.** The docs are the design memory; a PR that changes
   behaviour without changing docs is incomplete.
3. **`channelId` on every row and every message.** Queries are channel-scoped by default. `schema:check` holds every `CREATE TABLE` to it: `channel_id … NOT NULL`, or a reason in [`tables.json`](docs/architecture/schemas/tables.json). Every service owns ONE Postgres schema named after it (EP-07.6) — a table name is unique per owner, never shared across services.
4. **Every write goes out through the outbox**; every consumer is **idempotent**.
5. **Authorization is enforced server-side** via `can()` from `@atlas/policy`. Client-side checks are
   UX only, never the boundary.
6. **Mutating actions emit an audit event with a field-level delta.** That event is [`audit.recorded`](docs/architecture/schemas/events/audit.recorded.payload.schema.json), in the SAME transaction as the change, and MAM's `commitWith` is the pattern: every write, including the ones that announce no domain event. This was not true until EP-19.2 — `asset.updated` named the changed fields and nothing carried the values.
7. **Correctness-critical code — HSM file operations, send-to-air export, the authorization
   evaluator — needs 2 reviewers + pairing and is explicitly NOT AI-fast-tracked.** If your task
   touches these, say so and slow down.

## 6. Toolchain — and traps already paid for

**Stack:** Node LTS + TypeScript, **ESM only**, built-in `node:test` runner, `tsx` for execution.
**Libraries have no build step** — `exports` points straight at `./src/index.ts`.

Do not "modernise" any of the below. Each was decided or discovered the hard way:

| Trap                                                                          | What to do                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Imports carry explicit `.ts` extensions**                                   | Correct — required by the no-build-step design. `allowImportingTsExtensions` is on. Don't strip them.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Cross-package imports must use `@atlas/*`, never relative paths**           | `reference/` uses `../../contracts/src/index.ts` because it predates workspaces. **Rewrite on lift** and declare the dep in `package.json` — BOTH halves matter: with `useInferencePlugins: false` Nx builds this graph from `package.json` declarations, so an undeclared `@atlas/*` import produces **no graph edge** even though it resolves and typechecks, and a relative one never could. Either silently disables consumer-fanout CI for that package. **Enforced**: `npm run graph:check` (in `verify` and in CI, ahead of `nx affected`) diffs every import in `src/` and `test/` against the graph Nx actually computes, and names the fix.                                                                                                |
| **`ajv` must be imported by full specifier**                                  | Use `ajv/dist/2020.js` for **both** value and types. A bare `'ajv'` can resolve to an **ajv 6** copy (pulled in by eslint), whose `export =` typings are incompatible with v8's class. Runtime is fine; it fails only at type-time. Which copy is hoisted **changes with any dependency update** — ajv@8 sits at the root today, ajv@6 did before — so keep the full specifier regardless of what `node_modules` currently looks like. See [`libs/contracts/README.md`](libs/contracts/README.md).                                                                                                                                                                                                                                                   |
| **`docs/` is excluded from prettier**                                         | Deliberate — the markdown has hand-aligned tables that prettier would reflow. Don't remove it from `.prettierignore`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **A missing graph edge can be correct**                                       | `messaging` genuinely does not import `contracts` (it is schema-agnostic). Don't "fix" it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| **Nx Cloud is off**                                                           | Required: the build must work air-gapped ([FR-PLat-7](docs/requirements/05-functional-requirements.md#platform)). Keep caching local.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Every message id must be globally unique (a ULID)**                         | JetStream dedupes on `msgID` across the **whole stream**, not per subject. Reuse an id — a counter, a natural key, `'m1'` in a test — and `publish()` resolves successfully while the message is **silently discarded**. Pinned by a test in [`libs/messaging-nats`](libs/messaging-nats/).                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **A JetStream durable is a shared cursor**                                    | Durables are named per **(service, pattern)**. Two instances of one service sharing a durable is right (work splits). Two _different_ services sharing one means each steals half the other's events. Never name a durable after the pattern alone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Consumer dedup depends on what the consumer DOES**                          | There is no single right `SeenStore`, and the two wrong choices fail in opposite directions. A consumer that **writes to a database** needs a durable store whose mark commits **inside the handler's transaction** (`SqliteSeenStore`/`PgSeenStore.mark(tx, id)`): claim on a separate connection, crash mid-handler, and the id is marked for an effect that never happened — every redelivery is then suppressed and the message is lost. A consumer that **fans out to its own connections** (the websocket bridge) needs the per-process `InMemorySeenStore`: sharing dedup across those replicas means replica A claims the message and replica B's clients silently never receive it. Neither mistake shows up in a single-replica unit test. |
| **`@types/node` tracks the RUNTIME, not `latest`**                            | We run Node **24** (engines, CI `node-version`, `node:24-alpine`), so the types are pinned `^24`. Dependabot will offer 26; that is one major AHEAD and types APIs the runtime does not have. Two majors behind is equally wrong — it hides APIs that exist. Move this pin when the runtime moves, not before.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **`npm run verify` must include `npm ci --dry-run`**                          | CI installs with `npm ci`, which REFUSES a `package-lock.json` out of sync with `package.json`. Nothing else locally does — `npm install` silently reconciles the two, so an override or a dep edit passes every local check and fails CI in 6 seconds. That is what `lock:check` is for; do not drop it from `verify`.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **No `baseUrl`/`paths` in any tsconfig**                                      | `baseUrl` is deprecated in TS 6 and stops working in 7. It only ever anchored `paths`, and `paths` was dead config: these are npm workspaces, so `@atlas/foo` resolves via the `node_modules` symlink and that package`s own `exports`. Do not add either back to "fix" an import — fix the workspace dep instead.                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Studio runs its own toolchain — don't unify it**                            | `@atlas/studio` needs TypeScript **6.0** (Angular 22) while the libs are on **5.9**, uses **vitest** not `node:test` (component tests need a DOM), and has its own `tsconfig.json` rather than extending `tsconfig.base.json`. The strictness is reproduced there explicitly. See [its README](apps/studio/README.md).                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Studio is the only project that emits**                                     | So `allowImportingTsExtensions` alone is illegal there; it also needs `rewriteRelativeImportExtensions`. And ignore patterns must be `**/dist/**`, not `dist/**`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **Never use a constructor parameter property**                                | Containers run `node src/main.ts` on Node's **strip-only** TypeScript support, which refuses syntax that _emits_ code — parameter properties, enums, namespaces. One anywhere in a service's import graph breaks **every** container at startup while every test still passes. Enforced by eslint.                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Container images copy nested `node_modules`**                               | Not just the root. `ajv@8` lives in `libs/contracts/node_modules` because eslint hoists an incompatible `ajv@6` to the root; an image with only the root tree dies on a package npm installed correctly. See [`infra/docker/Dockerfile`](infra/docker/Dockerfile).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **In Studio, lenient `can()` is CORRECT**                                     | The opposite of the service rule. Studio decides what to _show_; the service enforces. `canEnforce` in the UI would hide legitimate controls whenever a check runs before the resource loads. Use `canStrict()` only for destructive actions with full context.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **An undefined CSS token fails SILENTLY**                                     | `var(--nope)` is invalid at computed-value time, so it falls back to the INHERITED value rather than erroring. Three Studio panels shipped reading 14 tokens `styles.scss` never declared — invisible buttons, un-red errors, and lint/typecheck/tests green throughout. Declare every token in BOTH palettes. `--color-<s>` is a foreground and `--color-<s>-bg` its tint; never fill a control with a status colour and write on it in `white`, because the palette inverts between themes.                                                                                                                                                                                                                                                        |
| **`dir="rtl"` mirrors the whole grid**                                        | Grid and flex tracks run along the INLINE axis, so RTL moves Studio's activity bar and side bar to the right of the screen. Anything measured in physical pixels must flip with it — the side-bar drag delta and its arrow keys multiply by `direction() === 'rtl' ? -1 : 1`, or the side bar shrinks when you drag it open. Prefer logical properties; `box-shadow` has no logical form and needs a `[dir='rtl']` rule.                                                                                                                                                                                                                                                                                                                             |
| **Studio's i18n is RUNTIME, not `$localize`**                                 | `LocaleService` fetches `/locales/<l>.json` and switches on a signal. Angular's `i18n` block in `angular.json` is the opposite — BUILD-time, one bundle per locale from XLIFF/ARB — so do not add one: a stale `i18n.locales.ar` pointing at the runtime map was dead config that `ng build --localize` would reject. The **assets** entry copying `src/locales` is what makes translations work, and a missing key renders as the key itself rather than as an error.                                                                                                                                                                                                                                                                               |
| **A file in `generated/` must be in `SPECS`**                                 | `rim.types.ts` carried the `GENERATED … DO NOT EDIT` banner while `scripts/generate-api-types.mjs` listed only iam and mam, so `api:check` never compared it for a month — and it had drifted, typing three optional contract fields as required (hence `NaN GB` in the ingest queue). Adding a Studio client for a service means adding its contract to `SPECS` in the same change.                                                                                                                                                                                                                                                                                                                                                                 |
| **Never read back inside a transaction**                                      | On `node:sqlite` an uncommitted write is visible to the same connection; on Postgres it is not visible outside the transaction's own client. A read-after-write inside the unit of work therefore **passes every test and returns stale data in production**. Read before the transaction, or through the tx client.                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **`await` the handler inside its `try`**                                      | `return fn()` from a `try` settles the promise after the `catch` is out of scope, so every rejection escapes to Fastify's default handler as a bare 500 — losing the problem document, the status code and the correlation id. `return await fn()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **The gateway forwards BYTES; it must not parse a body**                      | Fastify's default JSON parser rejects an EMPTY body, and `POST /assets/{id}/approve` with `content-type: application/json` and no body is the normal case — it 500'd at the gateway before ever reaching MAM. It also re-serialized (breaking any checksum) and refused every non-JSON upload. Catch-all buffer parser.                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Log every 5xx where it is raised**                                          | A 500 is deliberately opaque to the caller, so it is invisible to the operator too unless the service logs it with the same correlation id. An unlogged 500 in a cluster is undebuggable — you cannot even tell which pod raised it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| **Wait for the database at startup, within a budget**                         | Exiting when Postgres is not up yet hands the problem to Kubernetes' restart backoff, which grows to 5 minutes: measured, 7 restarts and 16 minutes to ready on a fresh install. Retry — but bounded, or a wrong password retries forever and never reports itself.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Refresh the token SINGLE-FLIGHT**                                           | IAM rotates refresh tokens and treats a reused one as a breach signal, revoking the whole family — every session, everywhere. Two concurrent refreshes is the NORMAL case when a token expires mid-load, so an in-flight refresh must be shared, never restarted. Same shape as MAM`s `PolicyClient`.                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Studio never persists a token**                                             | `localStorage`/`sessionStorage` are readable by any script on the origin, so persisting the REFRESH token gives one XSS a long-lived credential. A reload signing the user out is the deliberate, safer trade; the real fix is an httpOnly cookie from IAM and it is a server change.                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **Every login path does ONE argon2 verification**                             | Returning early for a refused account — locked, disabled, SSO-only — costs microseconds while a wrong password costs ~100ms, so the timing says "this account exists" even though the response is identical. Lockout (#240) made it worse: an attacker can CAUSE the lock and read the timing to confirm a username. Verify first, branch after. Pinned by a test that fails if the order is restored.                                                                                                                                                                                                                                                                                                                                               |
| **A client-settable header is INPUT, even an "internal" one**                 | `x-correlation-id` is the one internal header the gateway adopts from the client, and it was adopted verbatim — any string, straight into every service's log line for that request, the outbox headers and an audit record whose contract says ULID. A test even pinned `'corr-from-studio'` as the expected value, and another had loosened `isUlid(...)` to `\|\| length > 0` around it. Validate the shape at the edge and mint on failure; do not widen the assertion.                                                                                                                                                                                                                                                                          |
| **The broker body is the ENVELOPE, and the payload is inside it**             | Every producer publishes `body: buildEnvelope({ type, channelId, payload })`, so a consumer reads `body.payload.<field>`, never `body.<field>`. The websocket bridge read one level too high, and its test — which published a bare payload — agreed with it. Type the body as `Envelope<EventPayloads['<type>']>` (generated, EP-02.3) and the level is a compile error; and make a consumer test publish through `buildEnvelope`, so it can only pass against what a producer actually sends.                                                                                                                                                                                                                                                      |
| **Studio's specs share ONE jsdom — and one `localStorage`**                   | `@angular/build:unit-test` defaults `isolate: false` "to align with the Karma/Jasmine experience", so every spec file runs in the same environment. `EditorStore` persists the workspace to `localStorage` and restores it on construction, so a tab one spec file opens is silently already open when the next file starts — which files share a worker is scheduling, so it passed on every PR and failed on `main` (`expected 1 to be 2`). `src/test-setup.ts` clears storage before every test; a spec cannot guard against what another file left behind, so do not fix a leak like this in one spec.                                                                                                                                           |
| **A route table nothing builds a `Router` from is untested**                  | `app.routes.ts` carried `canMatch` on the `''` redirect since EP-11.2; Angular 22 refuses the pair at router construction (NG04014 — a redirect happens before any guard runs) and Studio crashed at startup while every spec stayed green, because each panel spec drives its component and the guards were tested as functions: nothing ever handed the real `routes` to `provideRouter`. `app.routes.spec.ts` now does, and navigates the three cases (signed out → `/signin`, granted → `/media`, ungranted → the catch-all). A config the framework validates at construction is only tested by constructing it.                                                                                                                                |
| **`rollout status` returning is not "reachable through the Service"**         | The pod is Ready a few hundred ms to a couple of seconds before kube-proxy has programmed its endpoint; a request through the gateway in that window is `502 upstream "mam" unreachable`. Smoke went red on `main` on a docs-only commit: MAM listening at :10.995, the suite's first MAM-bound request at :11.978, three 502s, then green — 0 restarts. Gate on the upstream answering THROUGH the gateway (the smoke suite's third test), not on `kubectl` saying Available.                                                                                                                                                                                                                                                                       |
| **An `enum` in a schema is a Tier-0 decision; make it once, in `common`**     | Code branches on a Tier-0 enum; an operator manages a Tier-1/2 list. An `enum` keyword looks identical either way, so the decision is written down: `docs/architecture/schemas/tier0-enums.json`, by pointer, with the reason — and `tier0:check` fails on a new enum that is not there. A shared enum is ONE `$def` in `common.schema.json`, `$ref`'d from every site: "task kind" was three inline copies with two value sets, and the BMS event could not express a task the BMS workflow definition allowed. If the honest reason is "an operator should manage this", it is not an enum.                                                                                                                                                        |
| **The gateway's routing table is in `main.ts`; `defaultRoutes` is a fixture** | `apps/api-gateway/src/routing.ts` exports `defaultRoutes`, listing services that do not exist (hsm, mts, scheduling) — it is what `buildGateway` uses in TESTS. Production builds its own table in `main.ts` from config origins. A route added to the fixture and not to `main.ts` is one the deployed gateway has never heard of; EP-19.1 shipped exactly that and found it when smoke got the gateway's own `no route` 404. The upstream gate now fails fast on that 404 and names the file.                                                                                                                                                                                                                                                      |
| **No identity is ever a metric label**                                        | `/metrics` is unauthenticated, so a `username` label publishes the account list to anyone who can reach the port — and a login spray writes its guesses into a document it can read back. It is the same bug as the cardinality one: a series per guess. Who failed is an AUDIT question; how many failed is the metrics question, and its label set is closed (#205).                                                                                                                                                                                                                                                                                                                                                                               |

## 7. Commands

```bash
npm install                       # workspace install (npm workspaces)

npx nx run-many -t lint typecheck test    # verify everything
npx nx test @atlas/contracts              # one project
npx nx affected -t test                   # only what your change impacts
npx nx show projects
npm run format                            # prettier --write
npm run graph:check                       # every cross-package import is a graph edge (consumer fanout)
npm run tier0:check                       # every schema enum is inventoried (Tier 0) and defined once
npm run schema:check                      # every table has one owner, both adapters, channel_id or a written reason
npx nx g service <name> --db --broker     # a new service, compliant and wired in (tools/generators/README.md)
npm run verify                            # lock, format, api, graph, lint, typecheck, test — what CI runs

node scripts/seed-github-backlog.mjs      # dry run; --execute to create issues
```

**Always run `npx nx run-many -t lint typecheck test` before committing.** Strict TS
(`noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax`) catches real bugs
in lifted code — expect a few, and fix them properly rather than loosening the config.

## 8. How to lift a package from `reference/`

This is the dominant task shape in Phase 0. Recipe:

1. `cp -r reference/<pkg>/{src,test,README.md} libs/<pkg>/`
2. Add `package.json` (name `@atlas/<pkg>`, `type: module`, `exports` → `./src/index.ts`, scripts
   `test` / `typecheck` / `lint`) and `tsconfig.json` extending `../../tsconfig.base.json`.
   Copy an existing one from [`libs/contracts`](libs/contracts/) — do not invent a new shape.
3. **Rewrite relative cross-package imports to `@atlas/*`** and declare those deps in `package.json`.
4. `npm install`, then `npx nx run-many -t lint typecheck test`.
5. Fix strict-mode findings properly. Run `npm run format`.
6. Mark the row ✅ in [`libs/README.md`](libs/README.md).
7. Verify the graph picked up the new edges:
   `npx nx graph --file=graph.json` and inspect `graph.dependencies`.

Paths note: `reference/<pkg>/src/` and `libs/<pkg>/src/` are the **same depth** from the repo root,
so `../../../docs/...` references keep working unchanged.

## 9. Committing

- Branch off `main`: `<type>/<issue-number>-<slug>`. Never commit directly to `main`.
- **Conventional commits**, scoped to the story: `feat(EP-04.1): lift service-kit into libs/`.
  Types are the usual set plus **`spike`** (a spike is a first-class work item here, and CI
  enforces the list — see [`ci.yml`](.github/workflows/ci.yml)).
- Explain **why** in the body, not just what — especially any decision or trade-off.
- `Closes #<number>` — only if the story is _fully_ done. If partial, use `Refs #<n>` and say what
  remains. (EP-03 closed only `#66` for exactly this reason.)

## 10. When you are unsure

- **A design question** → the answer is almost certainly already in `docs/`. Search before deciding.
- **A genuine gap in the design** → resolve it in the docs _first_, then implement. Don't encode a
  decision only in code.
- **An architectural choice with lasting consequences** (broker, storage engine, framework) → stop
  and ask the human. Spikes exist for this: they are time-boxed and **must produce a written
  artifact**, never just "we looked into it".
- **Report honestly.** If tests fail, say so with output. If you did part of a story, say which part.
  Never mark something done that you have not verified.

---

_Maintained by hand — update §3 whenever the project state moves._
